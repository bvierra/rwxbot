extends layout

block body
  div.channels
    ul
      - each channel in channels
        li
          a(href= '/logs/' + encodeURI(channel) + '/latest') #{channel}
          = ' ('
          a(href= '/logs/' + encodeURI(channel) + '/index') index
          = ' | '
          a(href= '/logs/' + encodeURI(channel) + '/latest') latest
          = ' | '
          a(href= '/logs/' + encodeURI(channel) + '/urls') URLS
          = ')'
